{
  "name": "quotes-generator-mern",
  "private": true,
  "version": "1.0.0",
  "description": "Full-stack quotes generator (React, Node, Express, MongoDB) with glass UI theme (Tailwind).",
  "scripts": {
    "dev": "npm-run-all --parallel server client",
    "server": "cd server && nodemon src/server.js",
    "client": "cd client && npm run dev"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5",
    "nodemon": "^3.1.0"
  }
}